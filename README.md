# data_simulator

Simulates features' values to be used as synthetic bulk metabolomics data. Such data serves to perform tests of statistical comparisons.

* The generation of values is simple, uses intervals and probability distributions (e.g. gamma).
* Creates 'a' and 'b' groups of samples, options are tunable. 

author: J Galvis 2023
